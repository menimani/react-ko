{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node", "vitest/globals"]
  },
  "include": [
    "**/*.test.ts",
    "**/*.test.tsx",
    "fixtures/**/*.ts",
    "fixtures/**/*.tsx"
  ],
  "exclude": ["consumers", "declarations"]
}
