{
  "name": "react-ko-repo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "src/react-ko"
  ],
  "scripts": {
    "test": "npm run test --workspace=src/react-ko",
    "build": "npm run build --workspace=src/react-ko"
  }
}
